#include <stdio.h>

int main() {
    char d[256];
    int dia, dia2, h, m, s;

    scanf("%s %d", &d, &dia);
    scanf("%d : %d : %d");
    scanf("%s %d", &d, &dia2);
    scanf("%d : %d : %d");

    int dt, ht, mt, st;
    
    return 0;
}